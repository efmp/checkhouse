<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emaza.checkhouse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ListaSolicitudes"></activity>
        <activity android:name=".alertas_verificacion" />
        <activity android:name=".iniciar_verificacion" />
        <activity android:name=".restablecer_contrasenia" />
        <activity android:name=".login"></activity>
        <activity android:name=".Maps"></activity>
        <activity android:name=".BuscarUsuario" />
        <activity android:name=".foto" />
        <activity android:name=".agregar_foto" />
        <activity android:name=".Act_CompararDirecciones" />
        <activity android:name=".modificar_contrasenia" />
        <activity android:name=".lista_de_verificaciones" />
        <activity android:name=".lista_de_solicitudes_vacia" />
        <activity android:name=".Lista_de_solicitudes_terminada" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".ingresar_direccion"
            android:label="@string/title_activity_maps" />
        <activity android:name=".sucessful_access" />
        <activity android:name=".crearcuenta_3" />
        <activity android:name=".crearcuenta_2" />
        <activity android:name=".crearcuenta_1"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyCpI6bfUbC9hfF6skoygMG6O4UT1uBq0Ss" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>